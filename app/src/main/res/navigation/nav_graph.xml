<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/navigation_map">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.example.safewalk.ui.home.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/navigation_map"
        android:name="com.example.safewalk.ui.map.MapFragment"
        android:label="Map"
        tools:layout="@layout/fragment_map" />

    <fragment
        android:id="@+id/navigation_report"
        android:name="com.example.safewalk.ui.report.ReportFragment"
        android:label="Report"
        tools:layout="@layout/fragment_report">
        <action
            android:id="@+id/action_navigation_report_to_reportPreviewFragment"
            app:destination="@id/reportPreviewFragment" />
    </fragment>

    <fragment
        android:id="@+id/reportPreviewFragment"
        android:name="com.example.safewalk.ui.report.ReportPreviewFragment"
        android:label="Report Preview"
        tools:layout="@layout/fragment_report_preview">
        <argument
            android:name="category"
            app:argType="string" />
        <argument
            android:name="description"
            app:argType="string" />
        <argument
            android:name="imageUri"
            app:argType="string"
            app:nullable="true" />
        <argument
            android:name="latitude"
            app:argType="float" />
        <argument
            android:name="longitude"
            app:argType="float" />
        <argument
            android:name="locationName"
            app:argType="string"
            app:nullable="true" />
    </fragment>

    <fragment
        android:id="@+id/navigation_reports_list"
        android:name="com.example.safewalk.ui.report.ReportsListFragment"
        android:label="Reports"
        tools:layout="@layout/fragment_reports_list" />

    <fragment
        android:id="@+id/navigation_profile"
        android:name="com.example.safewalk.ui.profile.ProfileFragment"
        android:label="Profile"
        tools:layout="@layout/fragment_profile" />
</navigation>